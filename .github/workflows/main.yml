name: Deploy to S3

on:
  push:
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Nix
        uses: cachix/install-nix-action@v25
        with:
          nix_path: nixpkgs=channel:nixos-unstable

      - name: Build Site
        run: nix develop --command build

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Deploy to S3
        run: |
          # 1. Assets (Images/Fonts) - Cache for 1 Year
          # Can bust the cache with query string or by changing image name
          # Commented for now because no images
          # aws s3 sync ./_site/img s3://${{ secrets.AWS_BUCKET_NAME }}/img \
          #   --delete \
          #   --cache-control "max-age=31536000,public"

          # 2. HTML, JS, CSS - cache up to 5 minutes in shared cache
          # Only 1 minute in browser cache
          aws s3 sync ./_site s3://${{ secrets.AWS_BUCKET_NAME }} \
            --delete \
            --exclude "img/*" \
            --cache-control "s-maxage=300,max-age=60,public"
      - name: Purge Cloudflare Cache
        run: |
          curl -X POST "https://api.cloudflare.com/client/v4/zones/${{ secrets.CLOUDFLARE_ZONE_ID }}/purge_cache" \
          -H "Authorization: Bearer ${{ secrets.CLOUDFLARE_API_TOKEN }}" \
          -H "Content-Type: application/json" \
          --data '{"purge_everything":true}'
